package otros;

import javax.swing.table.DefaultTableModel;
/*
public class MiModeloTabla extends DefaultTableModel {
    // Puedes agregar lógica adicional aquí según tus necesidades
    
    /**
	 * 
	 *//*
	private static final long serialVersionUID = 1L;

	@Override
    public Class<?> getColumnClass(int columnIndex) {
        // Indicar que la última columna debe utilizar el renderer personalizado
        if (columnIndex == getColumnCount() - 1) {
            return ColumnaBuscar.class;
        }
        // Devolver la clase por defecto para las demás columnas
        return super.getColumnClass(columnIndex);
    }
}
*/